# Reference: HUTCHAIN 3 — NOT ANALYSED

**Status: blocked.** The bytes have never reached the environment where the edit runs, so
there is no analysis here. This file exists so the gap is visible rather than silent.

- Required by: `SPORSHO/QC/PRE-DELIVERY-CHECKLIST.md` → "Every B-roll insert illustrates
  the sentence it sits under", and the hook rule — both of which the reference would inform.
- Attempted 2026-09-19 and 2026-09-21: `D:` is not mounted here and no copy of the folder
  was ever uploaded (uploads to this repository land as LFS pointers — see
  `SPORSHO/MEMORY/FAULTS.md`, entry 1).

When the files are reachable, fill this in using OpenMontage's 5-aspect shape
(`SPORSHO/REFERENCES/README.md`): Summary, Subject, Subject Motion, Scene (overlays
separately), Spatial Framing, Camera, plus "What Makes It Work" and the emulation cost.
