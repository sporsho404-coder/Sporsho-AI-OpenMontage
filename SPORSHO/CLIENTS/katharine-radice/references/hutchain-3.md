# HUTCHAIN 3 — Reference Notes (client-supplied)

- **Given as:** style reference only. "Make mine feel like this", not "copy this".
- **Where the files are:** client machine, `HUTCHAIN 3` folder on `D:\SPORSHO AI\`
  (`LOCAL-DRIVE.md`). **Not in this repository** — `REFERENCES/README.md`: metadata only,
  never video files.
- **Analysed:** **No.** Not yet, in any session. The bytes have never been reachable from
  this sandbox, so no shot list, pacing measurement, or transition read exists.

---

## What Is Known (and its limit)

Only what the brief says about it: the client wants the *register* of these pieces —
premium, calm, high-end fintech/trading feel, deliberate typography, transitions that
clarify rather than animate.

That is a **description**, not an analysis. Anyone reading this file must not treat it as
one. No timing, frame count, or structural claim about HUTCHAIN 3 has been measured.

## What To Do When The Files Are Reachable

Per `SPORSHO/REFERENCES/README.md` and `skills/meta/video-reference-analyst.md`:

```
1. python -c "from tools.analysis.video_analyzer import VideoAnalyzer; ..."   # or the documented CLI
   → transcript + scene boundaries + motion read + representative frames
2. Write SPORSHO/REFERENCES/hutchain-3/ANALYSIS.md   (OpenMontage's 5-aspect shape)
3. Write SPORSHO/REFERENCES/hutchain-3/PATTERNS.md   (generalised, reusable)
4. Add the INDEX.md row
5. Then, and only then, quote the reference in a client rule — as a measured number
   ("holds average 4.6s"), never as a mood word
```

## Guardrails While It Stays Unread

- **Do not** paraphrase this reference into on-screen copy or structure.
- **Do not** claim in a QC note that the cut "matches the reference". Nothing was measured.
- The style gap is currently covered by `styles/premium-minimalist.yaml`, which is a
  *documented* playbook in this repo with real numbers — and is labelled provisional in
  `BRAND.md` until the client's own brand and references are analysed.
